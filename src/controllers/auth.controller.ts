import { NextFunction, Request, Response } from "express";

export const registerUser = (
	req: Request,
	res: Response,
	next: NextFunction
) => {};

export const loginUser = (
	req: Request,
	res: Response,
	next: NextFunction
) => {};
