export const enum UserRoles {
	Creator = "creator",
	Eventee = "eventee",
}

export const enum EventType {
	Remote = "remote",
	Physical = "physical",
}

export const enum resourceStatus {
	Pending = "pending",
	Completed = "completed",
}
