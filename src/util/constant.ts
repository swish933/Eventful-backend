export const enum UserRoles {
	Creator = "creator",
	Eventee = "eventee",
}

export const enum EventType {
	Remote = "remote",
	Physical = "physical",
}

export const enum OrderStatus {
	Pending = "pending",
	Completed = "completed",
}
